---
name: qa-engineer
description: "Use this agent when you need to verify that code changes work correctly, run tests to validate functionality, or perform quality assurance checks. This includes running unit tests, end-to-end tests, API tests, or browser-based verification after implementing features or fixing bugs. Examples:\n\n<example>\nContext: The user has just finished implementing a new feature.\nuser: \"Add a shopping cart feature that calculates total price with tax\"\nassistant: \"I've implemented the shopping cart feature with the calculateTotal() function that adds items and applies tax. Let me now verify it works correctly.\"\n<commentary>\nSince a new feature was just implemented, use the qa-engineer agent to run tests and verify the shopping cart calculations work as expected.\n</commentary>\nassistant: \"Now I'll use the qa-engineer agent to run quality assurance on this implementation\"\n</example>\n\n<example>\nContext: The user asked to fix a bug in an API endpoint.\nuser: \"The /api/users endpoint returns 500 errors sometimes\"\nassistant: \"I've identified and fixed the null pointer issue in the users endpoint. Let me verify the fix works.\"\n<commentary>\nAfter fixing a bug, use the qa-engineer agent to test the API endpoint and confirm the fix resolves the issue without breaking other functionality.\n</commentary>\nassistant: \"I'll launch the qa-engineer agent to test this API endpoint and verify the fix\"\n</example>\n\n<example>\nContext: User wants to ensure existing functionality still works after refactoring.\nuser: \"Refactor the authentication module to use the new token format\"\nassistant: \"I've refactored the authentication module. Now I need to make sure everything still works.\"\n<commentary>\nAfter refactoring, use the qa-engineer agent to run the test suite and potentially do browser-based testing to ensure authentication flows still work correctly.\n</commentary>\nassistant: \"Let me use the qa-engineer agent to run the authentication tests and verify the refactor didn't break anything\"\n</example>"
model: opus
color: yellow
---
