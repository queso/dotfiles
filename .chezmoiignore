# Not targets: repo plumbing and script inputs.
README.md
bootstrap.sh
brew
tmux
LICENSE

# LazyVim bookkeeping: records which NEWS.md entries were read, and which
# extras were toggled in the :LazyExtras UI. The extras set is versioned in
# lua/config/lazy.lua instead, so this file is pure per-box state.
.config/nvim/lazyvim.json
{{ if ne .chezmoi.os "linux" }}
# Linux-only: systemd user units, and magi's ssh config.
.config/systemd
.ssh/config
{{ end }}
{{ if ne .chezmoi.os "darwin" }}
# macOS-only: launchd agent for the nightly re-add.
Library
{{ end }}
